# vim:filetype=sh

alias mv="mv -v"\
    cp="cp -v"\
    mkc="make clean && rm -f config.h"\
    mks="make && sudo make clean install"\
    YT="youtube-viewer"\
    fm="$FILES"\
    wup="ping -c 3 archlinux.org"\
    bat="cat /sys/class/power_supply/BAT0/capacity"

vf() { fzf | xargs -r -I % $EDITOR % ;}
