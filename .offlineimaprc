[general]
accounts = work
starttls = yes
ssl = yes
pythonfile = ~/.config/mutt/credentials/imappwd.py



[Account work]
localrepository = work-local
remoterepository = work-remote

[Repository work-remote]
auth_mechanisms = LOGIN
type = IMAP
remoteuser = 30031443@westernsydney.edu.au
remotepasseval = mailpasswd("work")
remotehost = outlook.office365.com
remoteport = 993
sslcacertfile = /etc/ssl/certs/ca-certificates.crt
folderfilter = lambda foldername: foldername in ['INBOX', 'Drafts', 'Service', 'Teaching', 'Professional', 'Research', 'Personal', 'TODO', 'Sent Items']

[Repository work-local]
type = Maildir
localfolders = ~/.mail/work
